specVersion: "v1"
serviceId: "stablecoindemo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "stablecoindemo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
